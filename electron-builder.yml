appId: com.cerumusic.app
productName: 澜音
directories:
  buildResources: build
asar: true
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!website/*'
  - '!scripts/*'
  - '!assets/*'
  - '!docs/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md,.idea,.kiro,.codebuddy}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
  - node_modules/ffmpeg-static/**
  - node_modules/better-sqlite3/**
  - node_modules/bindings/**
  - node_modules/file-uri-to-path/**
  - '**/*.node'
win:
  executableName: ceru-music
  icon: 'resources/icons/icon.ico'
  target:
    - target: nsis
      arch:
        - x64
        - ia32
    - target: zip
      arch:
        - x64
        - ia32
  fileAssociations:
    - ext: cmpl
      name: 澜音播放器（歌单/播放列表）文件
      description: 澜音播放器加密压缩后的（播放列表/歌单）文件
      icon: resources/cmpl.ico
  protocols:
    - name: Ceru Music Protocol
      schemes:
        - cerumusic
nsis:
  include: build/installer.nsh
  license: EULA.txt
  artifactName: ${name}-${version}-win-${arch}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  installerIcon: 'resources/icons/icon.ico'
  uninstallerIcon: 'resources/icons/icon.ico'
  # 建议提供高分辨率的安装插图以解决高分屏模糊问题
  # installerHeader: build/installerHeader.png # 推荐尺寸: 150x57 (或倍数)
  # installerSidebar: build/installerSidebar.png # 推荐尺寸: 164x314 (或倍数)
  deleteAppDataOnUninstall: true
  displayLanguageSelector: true
  oneClick: false
  allowToChangeInstallationDirectory: true
  allowElevation: true
mac:
  icon: 'resources/icons/icon.icns'
  category: public.app-category.music
  target:
    - target: dmg
      arch:
        - x64
        - arm64
    - target: zip
      arch:
        - x64
        - arm64
  extendInfo:
    - NSDocumentsFolderUsageDescription: 需要访问文档文件夹来保存和打开您创建的文件。
    - NSDownloadsFolderUsageDescription: 需要访问下载文件夹来管理您下载的歌曲。
    - NSMicrophoneUsageDescription: 需要访问麦克风以支持录音相关功能。
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  notarize: true
dmg:
  artifactName: ${name}-${version}-${arch}.${ext}
  title: ${productName}
linux:
  icon: 'resources/icons'
  target:
    - target: AppImage
      arch:
        - x64
    - target: snap
      arch:
        - x64
    - target: deb
      arch:
        - x64
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}-linux-${arch}.${ext}
snap:
  artifactName: ${name}-${version}-linux-${arch}.${ext}
deb:
  artifactName: ${name}-${version}-linux-${arch}.${ext}
npmRebuild: false
publish:
  provider: generic
  url: https://update.cerumusic.top
  useMultipleRangeRequest: false
electronDownload:
  mirror: https://npmmirror.com/mirrors/electron/
