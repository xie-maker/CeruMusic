# 仓库标识 (owner/repo). 用于查询 GitHub Releases.
GITHUB_REPO=timeshiftsauce/CeruMusic

# 推荐: 创建 fine-grained PAT, 仅授予该仓库的 "Contents: Read".
# 不填会走匿名调用 (60 req/h/IP), Vercel 高峰期容易被限流.
GITHUB_TOKEN=

# 可选: 调整 release 缓存时长 (秒). 默认 60.
RELEASE_CACHE_TTL=60
